/**
 * Removed in the editorial redesign — the paper aesthetic doesn't take a
 * glowing cursor halo. Kept as a no-op so existing imports don't break.
 */
export function CursorGlow() {
  return null;
}
